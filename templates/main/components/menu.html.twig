<div>
    <div class="container flex header">
        <a class="btn btn-primary" href="{{ path('app_home') }}">Accueil</a>
          <div class="flex">
            {% if app.user %}
                <p class="btn">{{ app.user.userName }}</p>

                <a class="btn btn-primary" href="{{ path('app_logout') }}">Déconnexion</a>
{#            {% else %}#}
{#                <a class="btn btn-primary" href="{{ path('app_register') }}">S'enregistrer</a>#}
            {% endif %}
        </div>


    </div>
</div>