{% extends 'base.html.twig' %}

{% block title %}Modifier question{% endblock %}

{% block body %}
    <h1 class="h1 mb-3 font-weight-normal text-center">Modifier question</h1>

    {{ include('question/_form.html.twig') }}

    <div class="d-flex">
        {{ include('question/_delete_form.html.twig') }}
        <a class="btn btn-primary mx-2" href="{{ path('app_survey_show', {'id': question.survey.id}) }}">Retour</a>
    </div>
{% endblock %}
