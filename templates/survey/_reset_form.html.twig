<form method="post" action="{{ path('app_survey_reset', {'id': survey.id}) }}"
      onsubmit="return confirm('Voulez vous annuler la suppression de cet élément ?');">
    <input type="hidden" name="_tokenReset" value="{{ csrf_token('_tokenReset' ~ survey.id) }}">
    <button class="btn btn-primary mx-2">Reset</button>
</form>