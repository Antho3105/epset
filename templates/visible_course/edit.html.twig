{% extends 'base.html.twig' %}

{% block title %}Edit VisibleCourse{% endblock %}

{% block body %}
    <h1>Edit VisibleCourse</h1>

    {{ include('visible_course/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_visible_course_index') }}">back to list</a>

    {{ include('visible_course/_delete_form.html.twig') }}
{% endblock %}
