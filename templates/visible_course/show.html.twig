{% extends 'base.html.twig' %}

{% block title %}VisibleCourse{% endblock %}

{% block body %}
    <h1>VisibleCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visible_course.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visible_course_index') }}">back to list</a>

    <a href="{{ path('app_visible_course_edit', {'id': visible_course.id}) }}">edit</a>

    {{ include('visible_course/_delete_form.html.twig') }}
{% endblock %}
